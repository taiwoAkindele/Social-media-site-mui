import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "1760a5",
            light: "skyblue"
        },
        secondary: {
            main: "888",
        },
        otherColor: {
            main: "fff"
        }
    }
})